# Scikit-learn Pipeline
This is a clean ML workflow using Scikit-learn Pipeline, ideal for organizing and streamlining machine learning experiments. Use it when you need to manage complex workflows and ensure reproducibility.

* Data Preprocessing
* Model Selection
* Hyperparameter Tuning
* Pipeline Optimization

To run the experiment, execute: `python main.py`