# Scikit-learn Pipeline — clean ML workflow
This experiment demonstrates a clean ML workflow using Scikit-learn Pipeline. Use this approach when you want to streamline your machine learning workflow, making it more efficient and reproducible.

* Data preprocessing
* Model selection
* Hyperparameter tuning
* Evaluation metrics

To run the experiment, use: `python main.py`