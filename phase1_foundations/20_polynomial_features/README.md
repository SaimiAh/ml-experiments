# Polynomial Features and Underfitting
This experiment explores the use of polynomial features to address underfitting in machine learning models. Polynomial features are used when a linear model is too simplistic to capture the underlying relationships in the data.

* Polynomial features
* Underfitting
* Feature engineering
* Model complexity

To run the experiment, use: python main.py