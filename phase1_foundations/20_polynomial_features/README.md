# Polynomial Features and Underfitting
This experiment explores the use of polynomial features to address underfitting in machine learning models. Polynomial features are used when a linear relationship between the features and target variable is not sufficient, and a more complex relationship needs to be modeled.

* Polynomial transformation of features
* Underfitting and its causes
* Feature engineering techniques
* Model evaluation metrics

To run the experiment, use: `python main.py`