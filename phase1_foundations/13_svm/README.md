# Support Vector Machine — Intuition and Code
This is an ML experiment exploring the concept of Support Vector Machines (SVMs), a type of supervised learning algorithm used for classification and regression tasks, particularly effective in high-dimensional spaces or when the number of features exceeds the number of samples.

* Max-margin hyperplane
* Kernel trick
* Soft margin
* Overfitting prevention

Run the experiment using: `python main.py`