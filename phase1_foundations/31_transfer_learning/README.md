# Transfer Learning Experiment - Day 31
Transfer learning with pretrained models is a technique where a model trained on one task is reused as the starting point for a model on a second task. This is useful when the second task has limited data or when the tasks are similar.

* Pretrained models
* Fine-tuning
* Feature extraction
* Model reuse

To run this experiment, simply execute: `python main.py`