# Day 30: Recurrent Neural Network and Sequences
This experiment explores Recurrent Neural Networks (RNNs) for sequence data. RNNs are ideal for modeling temporal relationships in sequential data such as time series, speech, or text.

* Recurrent Neural Networks (RNNs)
* Sequence data
* Temporal relationships
* Long Short-Term Memory (LSTM)

To run the experiment, use `python main.py`