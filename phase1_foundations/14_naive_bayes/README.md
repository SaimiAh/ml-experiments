# Naive Bayes Text Classifier
This is a machine learning experiment using Naive Bayes for text classification. Use this when you need to classify text into predefined categories, such as spam/not spam emails.

* Naive Bayes algorithm
* Text preprocessing
* Feature extraction
* Classification

To run the experiment, use: `python main.py`