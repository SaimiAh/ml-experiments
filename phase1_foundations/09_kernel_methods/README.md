# Kernel Methods Experiment
This experiment explores the kernel trick, a technique used in machine learning to enable efficient computation of dot products in high-dimensional feature spaces. It's particularly useful when dealing with nonlinearly separable data or when computational resources are limited.

* Kernel functions
* Feature mapping
* Dot product computation
* Nonlinear dimensionality reduction

To run the experiment, use: `python main.py`