# K-Means Clustering from Scratch
K-Means Clustering is an unsupervised machine learning algorithm used to group similar data points into clusters. It's useful when you need to identify patterns or structure in unlabeled data.

* Clustering
* Centroids
* Euclidean Distance
* Iterative Optimization

Run the experiment using: `python main.py`