# Convolutional Neural Network Basics
This is an experiment on the basics of Convolutional Neural Networks (CNNs), a fundamental concept in machine learning. CNNs are suitable for image and video processing tasks, such as object detection and classification.

* Convolutional layers
* Activation functions
* Pooling layers
* Fully connected layers

To run this experiment, use the command: `python main.py`