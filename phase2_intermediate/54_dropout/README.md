# Dropout Regularisation Experiment
Dropout regularisation is a technique used to prevent neural networks from overfitting by randomly dropping out units during training. It is useful when working with large datasets and complex models.

* Overfitting prevention
* Random unit dropout
* Improved model generalisation
* Regularisation technique

To run the experiment, use: `python main.py`