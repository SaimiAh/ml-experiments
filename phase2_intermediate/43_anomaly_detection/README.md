# Anomaly Detection with Isolation Forest
Day 43 of ML Experiment

This experiment uses Isolation Forest for anomaly detection, suitable for identifying unusual patterns in data when the normal behavior is not well-defined. It is particularly useful when the dataset is high-dimensional or contains complex relationships.

* Isolation Forest algorithm
* Anomaly detection
* Unsupervised learning
* High-dimensional data

To run the experiment, use: `python main.py`