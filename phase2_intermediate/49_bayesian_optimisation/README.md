# Bayesian Optimisation for Hyperparameters
This experiment uses Bayesian optimisation to tune hyperparameters, which is useful when dealing with complex models and large search spaces, typically on day 49 of an ML project. 

* Hyperparameter tuning
* Bayesian optimisation
* Model performance
* Automated search

Run the experiment using: `python main.py`