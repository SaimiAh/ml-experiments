# Gradient Boosting — XGBoost explained
This is an ML experiment on Day 34, exploring the power of XGBoost, a popular gradient boosting library. XGBoost is suitable for handling large datasets, complex relationships, and a mix of categorical and numerical features.

* Gradient Boosting: an ensemble learning method
* Decision Trees: base models for boosting
* Hyperparameter Tuning: crucial for optimal results
* Overfitting Prevention: XGBoost's built-in regularization

To run the experiment, simply execute: `python main.py`