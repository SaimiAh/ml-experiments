# Feature Selection Techniques Compared
This experiment compares various feature selection techniques to identify the most effective methods for improving model performance. Use this when dealing with high-dimensional datasets where feature selection can significantly impact model accuracy and efficiency.

* Correlation-based feature selection
* Mutual information-based feature selection
* Recursive feature elimination
* L1-based feature selection

To run the experiment, use: 
`python main.py`