# Word2Vec Experiment
This is a machine learning experiment focusing on Word2Vec, a word embedding technique used to represent words as vectors in a high-dimensional space. Use Word2Vec when you need to capture semantic relationships between words in a text dataset.

* Word embeddings
* Vector space
* Semantic similarity
* Dimensionality reduction

To run the experiment, use: `python main.py`