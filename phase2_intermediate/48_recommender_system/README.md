# Recommender System - Collaborative Filtering
This is a machine learning experiment using collaborative filtering for a recommender system. Use it when you want to suggest items to users based on the behavior of similar users.

* User-item matrix
* Matrix factorization
* Nearest neighbors
* Rating prediction

To run the experiment, simply execute: 
```bash
python main.py```