# Model Interpretability — SHAP values
This experiment explores model interpretability using SHAP values, which help understand how features contribute to model predictions. Use this when you need to explain complex model decisions.

* SHAP values: assign values to features for each prediction
* Model interpretability: understanding model decisions
* Feature importance: ranking features by contribution

To run this experiment, use: `python main.py`