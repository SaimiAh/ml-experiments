# Zero-Shot Classification with Transformers (Day 77)
Zero-shot classification with transformers is an ML experiment that enables classification without prior training data. Use this approach when labeled data is scarce or unavailable.

* Transformer models
* Zero-shot learning
* Text classification
* Natural Language Processing

To run the experiment, execute `python main.py`