# Vector Databases Experiment
This experiment explores vector databases using Chroma and FAISS, ideal for similarity search and nearest neighbor queries, particularly in machine learning applications.

* Vector indexing
* Similarity search
* Nearest neighbor queries
* Efficient data retrieval

To run: `python main.py`